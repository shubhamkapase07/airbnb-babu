# Reference screenshots

The live reference (https://airbnb-clone-umber-two.vercel.app) is protected by a **Vercel Security
Checkpoint** *and* **Kasada BotID**, which block automated capture (see
`.claude/agents/reference-capture.md`). The **8 full-page screenshots supplied by the reviewer** are
therefore the source of truth for this clone and were used to drive every section and the
fidelity-check loop.

Drop those PNGs into this folder (e.g. `01-hero.png … 08-host.png`) to re-run the
`fidelity-check` skill / `pixel-perfect-reviewer` agent against them.
